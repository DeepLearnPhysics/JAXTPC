# Data types

The `NamedTuple` bundles that flow through the pipeline (see the
[Data model](../../architecture/data-model.md) and
[Config vs params](../../architecture/config-vs-params.md) for how they fit together).

::: tools.config
