# JAXTPC runtime dependencies.
# For GPU, install JAX per https://docs.jax.dev/en/latest/installation.html
# (e.g. `pip install -U "jax[cuda12]"`) instead of the plain `jax` below.
# Upper bound: JAX makes frequent breaking API changes; bound it so an unpinned
# CI/install can't silently pull an incompatible release (tested on 0.5.x-0.6.x).
jax>=0.4,<0.7
numpy
scipy
matplotlib
h5py
hdf5plugin          # required to READ production output (default blosc-zstd codec)
pyyaml
pillow              # viewer GIF/MP4 export

# Dev tooling
nbstripout           # clears notebook outputs on commit; run `nbstripout --install --attributes .gitattributes` once per clone

# Tests
pytest
